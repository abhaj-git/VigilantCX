# Copy to .env and set your keys. Used when not running via Streamlit Cloud.
# For LLM summaries: set at least one. Gemini is tried first; OpenAI is fallback.

# GEMINI_API_KEY=your-gemini-key
# OPENAI_API_KEY=your-openai-key
