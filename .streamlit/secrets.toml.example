# Copy to secrets.toml and fill in your keys. Never commit secrets.toml.
# For LLM summaries: set at least one key. Gemini is tried first; OpenAI is used as fallback (e.g. when Gemini hits quota).

# GEMINI_API_KEY = "your-gemini-key"
# OPENAI_API_KEY = "your-openai-key"
